const emptyArray = ['cabbage','tomato sauce','salmon']
//Array values
/*Arrays can contain
Primitives -> strings and numbers
Objects -> Objects, other arrays and even functions*/

const strings = ['one','two','three']
const numbers = [1,2,3,4]
const booleans = [true,false]
const objects = [{name:'Simon'},{name:'Dela'}]
const arrays = [[1,2,3],[4,5,6]]

//one item in one line
const groceriesToBuy= [
    'cabbage',
    'tomato sauce',
    'salmon'
]

//objects separated with new lines
const object = [
    {firstName: 'Simon', lastName: 'Dela'},
    {firstName: 'Qhouphi', lastName: 'Cmon'}
]

//checking numer of items in an array
console.log(numbers.length)

//setting the vakue of an item
strings[0] = 1